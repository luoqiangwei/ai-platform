帮我完成AI数据表、工作流程的设计和编码。比如AI回复的Prompt定义，以及AI再进行工作的时候，需要携带上相关的shot_memory、core_memory、task_memory，如果一次上下文可能带不完成，那可以多轮分别带上。

在我的想法中，当通过飞书事件接收到用户的消息时，需要包含几轮基础回复。
1. 确定用户的意图，这一部分需要返回json，并且将意图、需要使用的mcp、tools、skills、相关文档（知识库）的类型都筛选出来，然后ai-platform从数据库中查询对应工具的描述和调用方式。
2. 接下来，AI读取解决这类问题需要的相关文档，调用相关工具等来解决问题，它具有自己的操作空间，位于/aiworkspace目录。
3. 最后，json中包含针对用户的回复、需要更新的shot_memory、core_memory、task_memory、skills、agents等信息（不一定非要更新，只更新有必要的，所以这部分需要AI判断不需要的话，对应的json字段为空）。然后询问AI的可能有多个不同的用户，因此相关的task_memory肯定要针对用户进行不同的区分

AI的所有自更新数据放到sqlite中，所有的操作位于/aiworkspace目录
AI自己找的MCP、tools、skills、相关文档（知识库）的类型都放到数据库中，同时对应的工具放到/aistore/里面，里面创建不同的目录进行归类。

---------------
还需要AI具备搜索网络、查找skills、MCP等工具的能力，并更新到数据库中，并且在必要的时候使用这些工具解决用户的问题。

同时我们也需要一个区域，用于存放这个项目自己开发的MCP、Skills、Tools等、并打包到docker里，等到启动的时候扫描并把这些工具放入到数据库中。

现有的数据库也需要进行迭代，比如没有时间，没有分类、有效期、分类之类的列。


---------------
需要把AI每一步行为的调试信息都打印出来，尤其是发送给API的请求参数和返回信息，以及信息如何拆分的，我要看看它运作过程什么样，这些Debug信息可以作为.env的环境进行开关控制。

然后我发现它有几个问题
1. 还不会使用wget、浏览器、curl、cat、sh等工具，我们需要为它准备这些工具，并且让AI在必要的时候调用这些工具，docker中的/aiworkspace是它的工作区，它可以干任何事情。
2.还无法形成自己的下面这些表能力，我需要它能够自己进化，在遇到困难时能够自己查找和调用工具解决问题，但是当问题太过复杂时，就需要要求它向用户寻求帮助。
表2: core_memory
核心记忆，归纳聚合shot_memory，形成一段精炼的核心记忆。
同时别人和自己交流的态度等信息记录下来。
表3: task_memory
这张表记录了在和别人说话时，记录下要做的事情，结束时间，任务进度，下次自主出触发事件等信息
表4: agents
记录一些工具相关的信息
表5: skills
记录一些skills，包含用途、不能用在哪些场景等
表6: bootstrap
下一次自己做默写事情时，它的前置背景、注意事项、风险等
表7: headbeat
下一次激活任务的时间等信息
表8: identity
记录自己扮演什么样的身份，这个随时会变，每次变动都代表着自我认知发生了变化。
表9：soul
记录自己的一些核心信念
表10: tools
记录一些工具如何使用，比如ssh、相机、浏览器等
表11: user
记录自己对访问者的态度、想法等。

--------------------
意图分析不再发明工具名 ： AnalyzeIntent 限制 tools 字段只能从内置列表里选，否则必须输出空数组。见 ai_service.go 。

这一段，如果是空数组的情况下，增加一个阶段，让AI从github或者其他地方去下载对应的工具，但是需要检查是不是恶意工具。然后再尝试调用这个工具去解决问题，如果成功，类似ls、wget这种tool记录到tools表，如果是MCP工具解决成功，记录到mcp表，如果是skills，记录到skills表。如果失败5轮（可以通过环境变量控制最大论数），就要求用户自己去解决。

--------------------------
很好，可以查询天气了，但是现在有一个问题，核心记忆的存储有点同质化了。这里有两个问题

1. 需要在短期记忆里标柱哪些记忆被转换成了核心记忆，然后这些记忆下次不会再被使用了。下一次产生核心记忆的时候，需要携带上已有的核心记忆，和最近未被转换为核心记忆的短期记忆，确保不生成同质化的核心记忆，如果短期记忆过长，需要大模型进行提炼，最后再产生一条长期记忆。
2. 用户在询问问题时，将对应用户的最近5条短期记忆和核心记忆一起发送给AI，让AI根据这些记忆来回答用户的问题。
